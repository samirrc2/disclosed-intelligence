# Environment

Pinned, offline reproduction environment. `Dockerfile` mirrors the audited Code
Ocean capsule (`capsule/environment/Dockerfile`): Python 3.12.8 with
numpy 2.2.6, pandas 2.2.3, scipy 1.14.1, statsmodels 0.14.4, matplotlib 3.9.2,
pytest 8.3.3. Reproduction needs no network or API keys — see `reproduce.sh`.
