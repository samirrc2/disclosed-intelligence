# Analysis dependencies (offline reproduction; no API/network needed).
# Pinned to match environment/Dockerfile (Code Ocean py-r Python 3.12.8).
# numpy/pandas pair is fixed: numpy 2.4.x / pandas 3.0.x has a known regression.
numpy==2.2.6
pandas==2.2.3
scipy==1.14.1
statsmodels==0.14.4
matplotlib==3.9.2
pytest==8.3.3
